<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cursos del Circulo Infernal</title>
</head>
<body>
    <main class="container">
        <table class="table" border="1">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Duracion</th>
                    <th>Docente</th>
                </tr>
            </thead>
            <%
                if(Array.isArray(cursos && cursos.length > 0)){
            %>
            <tbody>
                <tr>
                    <% cursos.forEach(curso => { %>
                        <td><%= curso.id %></td>
                        <td><%= curso.nombre || '' %></td>
                        <td><%= curso.duracion || '' %></td>
                        <td><%= curso.docente || '' %></td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
        <% } else { %>
            <div>No hay cursos disponibles en este momento, existe un erro al cargarlos</div>
            <p><a href="./bienvenida.html">Volver a Bienvenida</a></p>
    </main>
    
</body>
</html>